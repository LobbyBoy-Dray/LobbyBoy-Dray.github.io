<section class="giscus mx-auto mt-8 max-w-4xl"></section>

<script src="https://giscus.app/client.js"
        data-repo="LobbyBoy-Dray/LobbyBoy-Dray.github.io"
        data-repo-id="R_kgDOKtmM0Q"
        data-category="Blog Post Comments"
        data-category-id="DIC_kwDOKtmM0c4Cb-Oj"
        data-mapping="url"
        data-strict="0"
        data-reactions-enabled="1"
        data-emit-metadata="0"
        data-input-position="bottom"
        data-theme="dark_dimmed"
        data-lang="en"
        data-loading="lazy"
        crossorigin="anonymous"
        async
></script>

<!-- const giscusScript = document.createElement("script");
Object.entries(giscusAttributes).forEach(([key, value]) => giscusScript.setAttribute(key, value));
document.body.appendChild(giscusScript);




function changeGiscusTheme() {
  let theme = document.querySelector('html[data-theme]').getAttribute('data-theme')
  function sendMessage(message) {
    const iframe = document.querySelector('iframe.giscus-frame');
    if (!iframe) return;
    iframe.contentWindow.postMessage({ giscus: message }, 'https://giscus.app');
  };
  sendMessage({
    setConfig: {
      theme: theme,
    },
  });
}

let element = document.querySelector('html[data-theme]');
let obsConfig = { attributes: true, attributeFilter: ['data-theme'] };
const callback = function(mutationsList, observer) {
for (const mutation of mutationsList) {
        if (mutation.type === 'attributes' && mutation.attributeName === 'data-theme') {
                // changeGiscusTheme();
        }
}
};
const observer = new MutationObserver(callback);
observer.observe(element, obsConfig); -->

